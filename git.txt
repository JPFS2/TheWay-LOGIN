Configurar informações do usuario e outras informações
git

Salvar dados 

git config --global user.name "Marlon Ferreira"
git config --global user.email
git config --global core.editor

Visualizar 
git config user.email
git config --lista

Inicialziar repositorio
git init

Branch (Versões do nosso codigo)
Commit (Enviar alterações)
README (Instruções e comentarios sobre o projeto)

git status - verifica o diretorio
git add -A adiciona todos os arquivos as modificações  
git add nome do arquivo - adiciona o arquivo especifico ao monitoramento 
git comit -m "mensagem"
git comit -am "mensagem" adiciona as modificações e comita ao mesmo tempo
git log - mostra todos os comites / tudo que foi feito 

o que interessa mesmo são os 7 primeiros caracteres do git
commit af2368a30572a78be87a47c5f94aef93d23f0c54

git branch - mostrar todas as branchs e o status

git reset - volta para o commit que eu indicar 
  - soft volta mas com as modificações sem estar comitadas 
  - mixed volta mas com as midificações antes do add
  - hard apaga tudo	 e fica igual a o comit que vc indicou 

criar novo branch 
git branch "nome"

trocar branch 
git checkout "branch" 

git diff - detalha as alterações no arquivo
git diff --name-only diz o nome dos arquivos que foram alterados

git checkout "BREANCH" --"nome do arquivo" - restara apenas um arquivo

git push origin master

para ignorar o envio de determinados arquivos é só criar um arquivo .gitignore
e colocar o nome do arquivo que deve ser ignorado 

git rebert --no-edit "codigo do comit" 





conectar externamente

ssh-keygen -t rsa -b 4096 -C "marlonfsantos@outlook.com.br" - gerar chave

git remote add origin https://github.com/marlonfsantos/B7WEB.git
git branch -M main

git push -u origin main 

git push origin teste - insere a branck
git push origin :teste - deleta a branch

git pull "diretorio de origen" "diretorio de destino"

git clone "diretorio" para clonar arquivos de um diretorio

---- para comtribuir com outros projetos tem que ser dar fork no projeto

clona , modifica 

depois da new push request